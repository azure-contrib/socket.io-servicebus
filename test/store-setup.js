var should = require('should');

var SbStore = require('../lib/sbstore.js');

describe("Store setup", function() {
  it("should be createable", function() {
    var store = new SbStore();
  });
});
  